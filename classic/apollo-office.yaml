substitutions:
  path: "office"
  hostname: "apollo-office"

packages:
  hardware: !include _hardware.apollo_msr2.yaml
  device: !include _device.apollo_msr2.yaml
  proxy: !include _device.apollo_msr2.ble_proxy.yaml
  beacon: !include _device.apollo_msr2.beacon.yaml
  tracking: !include _ibeacon.tracking.all.yaml